name: dependencies
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.2.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio:
  lottie:  
  shared_preferences:
  dartz:
  get_it:  
  cached_network_image: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter: